[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "google-genai>=2.6.0",
    "psycopg2-binary>=2.9.12",
    "pydantic",
    "sqlmodel",
    "uvicorn>=0.48.0",
]
